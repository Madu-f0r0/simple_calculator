#include "main.h"

/**
 * add - calculates the sum of two integers
 * @a: the first integer to be summed up
 * @b: the second integer to be summed up
 *
 * Return: the sum of the two integers
 */
int add(int a, int b)
{
	return (a + b);
}


/**
 * subtract - calculates the difference between two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the difference between the two integers
 */
int subtract(int a, int b)
{
	return (a - b);
}


/**
 * multiply - calculates the product of two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the product of the two integers
 */
int multiply(int a, int b)
{
	return (a * b);
}


/**
 * divide - calculates the dividend of two integers
 * @a: the integer to be divided
 * @b: the divisor
 *
 * Return: the dividend of the two integers
 */
int divide(int a, int b)
{
	return (a / b);
}
